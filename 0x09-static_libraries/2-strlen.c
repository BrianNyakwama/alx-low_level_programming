#include "main.h"
#include <string.h>

/**
  * _strlen - function that returns length of string
  * @s: string pointer to this function
  *
  * Return: length of string
  */
int _strlen(char *s)
{
	return (strlen(s));
}
