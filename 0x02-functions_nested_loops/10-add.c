#include "main.h"

/**
  * add - adds two integers and returns the result
  * @p: first number to be computed
  * @q: second number to be computed
  *
  * Return: Integer value of the sum
  */
int add(int p, int q)
{
	int r;

	r = p + q;

	return (r);
}
